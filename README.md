# STIX_LM
The `data` folder contains STIX scenarios with corresponding names. By running `process_data.py`, you will directly obtain the `AI_gen` folder, which includes results from zero-shot predictions and preliminary Chain of Thought (COT) analyses, all stored in JSON format.
